{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Accueil{% endblock %}

{% block body %}
    <h1>Accueil</h1>

    <div class="list-group list-group-flush">
        {% for article in articles %}
            <a href="{{ path('app_admin_article_index', {'id': article.id}) }}">
                {{ article.title }}
            </a>
        {% endfor %}
    </div>
{% endblock %}
